# Scripts

🇮🇹 Script utility per sviluppo e operations.
🇬🇧 Utility scripts for development and operations.

Esempi futuri / future examples:

- `bootstrap.sh` — setup ambiente di sviluppo
- `enroll-device.sh` — pairing CLI di un nuovo device
- `backup.sh` — snapshot di database e vector store
- `lint-all.sh` — esegue tutti i linter del monorepo
